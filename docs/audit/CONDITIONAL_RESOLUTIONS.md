# Conditional rules, and how each one resolved

Verdicts as the workers measured them on 2026-08-19 and into 2026-08-20, against the tree at
commit 7aa8ea1 and recorded in commit a72e5f6. They are a snapshot of that tree and are not
re-checked as it moves, so read every present-tense sentence below as "was true then". At least
one has already been overtaken: DEP-003 was fixed after the audit, and `lib/install.js` now
returns the real load result from `reloadLaunchAgent` while `bin/interstice.js` exits non-zero
when the LaunchAgent did not end up running, so the unconditional "loaded" line the row describes
no longer exists. Regenerate this file with `python3 docs/audit/merge_resolutions.py --conditionals`.

Conditional rows: 18. Each names its precondition and whether it held.

| row_id | sheet | precondition (from the manifest) | held? | applies? | verdict |
|---|---|---|---|---|---|
| `CM-009` | Process/Communication | the closing block produces a manual verification checklist, which is a guide; applies to that artifact only | no | no | NA |
| `CM-010` | Process/Communication | the closing block produces a manual verification checklist, which is a guide; applies to that artifact only | no | no | NA |
| `DEP-001` | Deployment | Interstice 'deploys' as a macOS LaunchAgent on one machine, not a server; each row must record whether its notion of deployment maps onto a LaunchAgent | yes | yes | PASS |
| `DEP-002` | Deployment | Interstice 'deploys' as a macOS LaunchAgent on one machine, not a server; each row must record whether its notion of deployment maps onto a LaunchAgent | yes | yes | PASS |
| `DEP-003` | Deployment | Interstice 'deploys' as a macOS LaunchAgent on one machine, not a server; each row must record whether its notion of deployment maps onto a LaunchAgent | yes | yes | FAIL |
| `DEP-006` | Deployment | Interstice 'deploys' as a macOS LaunchAgent on one machine, not a server; each row must record whether its notion of deployment maps onto a LaunchAgent | no | no | NA |
| `DEP-007` | Deployment | Interstice 'deploys' as a macOS LaunchAgent on one machine, not a server; each row must record whether its notion of deployment maps onto a LaunchAgent | yes | yes | PASS |
| `DEP-008` | Deployment | Interstice 'deploys' as a macOS LaunchAgent on one machine, not a server; each row must record whether its notion of deployment maps onto a LaunchAgent | no | no | NA |
| `DEP-011` | Deployment | Interstice 'deploys' as a macOS LaunchAgent on one machine, not a server; each row must record whether its notion of deployment maps onto a LaunchAgent | no | no | NA |
| `DEP-013` | Deployment | Interstice 'deploys' as a macOS LaunchAgent on one machine, not a server; each row must record whether its notion of deployment maps onto a LaunchAgent | no | no | NA |
| `DEP-014` | Deployment | Interstice 'deploys' as a macOS LaunchAgent on one machine, not a server; each row must record whether its notion of deployment maps onto a LaunchAgent | no | no | NA |
| `DEP-015` | Deployment | Interstice 'deploys' as a macOS LaunchAgent on one machine, not a server; each row must record whether its notion of deployment maps onto a LaunchAgent | no | no | NA |
| `DEP-016` | Deployment | Interstice 'deploys' as a macOS LaunchAgent on one machine, not a server; each row must record whether its notion of deployment maps onto a LaunchAgent | yes | yes | PASS |
| `DEP-017` | Deployment | Interstice 'deploys' as a macOS LaunchAgent on one machine, not a server; each row must record whether its notion of deployment maps onto a LaunchAgent | no | no | NA |
| `DEP-018` | Deployment | Interstice 'deploys' as a macOS LaunchAgent on one machine, not a server; each row must record whether its notion of deployment maps onto a LaunchAgent | no | no | NA |
| `DF-005` | Design/Design_Fidelity | applies if docs/design-immersive-reading.html is treated as the canonical design artifact; record whether the panel drifted from it | no | no | NA |
| `DF-006` | Design/Design_Fidelity | same precondition as DF-005: does a canonical design artifact exist here | no | no | NA |
| `VD-007` | Design/Visual_Design | applies only if phase 2 introduces a token layer; record which way it resolved | no | no | NA |

### Evidence per conditional row

- **`CM-009`** (NA): Precondition (from this row's own scope): the artifact under review is the manual verification checklist the goal loop's closing block produces, and the rule applies to that artifact only. It did not hold - that checklist does not exist. docs/GOAL_LOOP_STARS_READER_2026-08-17.md:820 carries the box 'Create a manual verification checklist for the user to go to in order to verify and check your work' and it is unchecked, and no such file exists under docs/ (the eight docs/*.md files are contracts, notes, measurements, goal loops and findings, none of them a user-facing verification guide). With no guide artifact in scope and no session transcript supplied, there is nothing to audit for click-by-click steps or literal hyperlinks.
- **`CM-010`** (NA): Same precondition as CM-009 and it resolved the same way: the guide this row scopes to, the closing block's manual verification checklist, was never produced (docs/GOAL_LOOP_STARS_READER_2026-08-17.md:820 is unchecked and no such file exists under docs/). With no user-facing imperative steps in scope, there is no step whose predecessor open-the-app context could be checked.
- **`DEP-001`** (PASS): Precondition: does this row's notion of deployment map onto a macOS LaunchAgent? It holds in reduced form: the only deploy target is the LaunchAgent job com.interstice.daemon, which runs source straight out of the working tree (~/Library/LaunchAgents/com.interstice.daemon.plist WorkingDirectory=/Users/scottlydon/Developer/interstice), so 'build' and 'cache-bust' are vacuous while 'restart' and 'behaviorally verified' do map, and the rule therefore applies to those two clauses. Verified live: newest source mtime is 2026-08-19 07:34:23 (lib/latency.js) versus daemon start 2026-08-19 21:17:27 (ps -o lstart= -p 859), `launchctl list` prints `859 0 com.interstice.daemon`, and GET http://127.0.0.1:7420/api/health returns 200 application/json. No backend source is edited-but-unshipped.
- **`DEP-002`** (PASS): Precondition: does this row's notion of deployment map onto a macOS LaunchAgent? It holds: the deployment act is `launchctl unload` then `load`, and lib/install.js:141-142 executes both itself rather than telling anyone to run them, so the rule applies and is met by the shipped path. The README Install block (README.md:52-59) does ask a human to run doctor/install/start, but that is first-time setup of an open-source tool on the reader's own machine, not a deployment step handed back after a code change.
- **`DEP-003`** (FAIL): Precondition: does this row's notion of deployment map onto a macOS LaunchAgent? It holds by mapping 'the response that claims the change is shipped' onto the one place this repo makes a shipped-claim: the console output of `interstice install`. It applies, and it fails there. lib/install.js:142-145 catches a launchctl load failure, prints `launchctl    load failed: <msg>`, and then unconditionally prints `launchctl    loaded` on the very next line; install() also returns true unconditionally (install.js:146) and bin/interstice.js:71 follows with 'Now run: interstice doctor'. The evidence line asserts a deployment outcome the command never verified.
- **`DEP-006`** (NA): Precondition: does this row's notion of deployment map onto a macOS LaunchAgent? It does not hold: there is no content-hashed asset layer at all. package.json has no build script and grep for vite/webpack/esbuild/rollup/tsc across package.json, .github/workflows, lib, bin and test returns nothing; web/panel.html, web/dashboard.html and web/debug.html are self-contained (grep for `<script src=` and `<link href=` in web/*.html returns zero hits) and are read off disk per request by serveFile (lib/server.js:563-567). The LaunchAgent analogue of the hard refresh does exist: assetStamp() (lib/server.js:61-67) returns panel.html's mtime and POST /api/panel/ping hands it to the open window (lib/server.js:131) so the page reloads itself when the source moves.
- **`DEP-007`** (PASS): Precondition: does this row's notion of deployment map onto a macOS LaunchAgent? It holds: the three entry documents have stable names (/, /panel, /debug) and are served over HTTP by the local daemon, so the rule applies. Verified live with `curl -D -`: /, /panel and /debug each return 200 text/html with `cache-control: no-store`, and every JSON route returns `cache-control: no-store` too (lib/server.js:31 and :566). No `immutable` and no `max-age` appears anywhere in the served headers, and there are zero local script or link URLs to hash-check because the HTML is fully inline.
- **`DEP-008`** (NA): Precondition: does this row's notion of deployment map onto a macOS LaunchAgent? It does not hold: the project ships no Chrome extension. There is no chrome/ directory in the tree (the .gitignore:8 entry `chrome/port.js` is vestigial), and no manifest.json, content script or service worker exists anywhere in the repo. The only Chrome involvement is two ordinary browser processes launched with --user-data-dir by lib/cdp.js:72 and lib/panel.js:212, which carry no extension.
- **`DEP-011`** (NA): Precondition: does this row's notion of deployment map onto a macOS LaunchAgent? It does not hold: the project ships no MCP server and no agent backend. grep for 'mcp', 'modelcontextprotocol' and 'get_secret' across the tree (excluding node_modules, .git and logs) returns only two prose mentions in docs/GOAL_LOOP_STARS_READER_2026-08-17.md:181 and docs/STARS_NOTES_2026-08-19.md:154. package.json declares one bin, ./bin/interstice.js, which is a CLI, not an MCP host.
- **`DEP-013`** (NA): Precondition: does this row's notion of deployment map onto a macOS LaunchAgent? It does not hold: nothing is hosted anywhere. `git remote -v` shows a single remote, origin -> https://github.com/scott-lydon/interstice.git, with no Render, Vercel or Firebase service; .github/workflows/ci.yml (the only workflow) has one job, `test`, that runs `node --test` on macos-latest and contains no deploy step or deploy hook. The daemon runs on 127.0.0.1:7420 under launchd on this machine only (lib/server.js:573 binds loopback).
- **`DEP-014`** (NA): Precondition: does this row's notion of deployment map onto a macOS LaunchAgent? It does not hold: there is no push that deploys, so there is no live commit to compare. The LaunchAgent executes bin/interstice.js from the working tree directly, so 'the live code' is whatever is checked out rather than whatever was last pushed, and daemon.health() (lib/daemon.js:127-147) exposes no commit or build stamp for a /version-style check. Reported for completeness: `git rev-parse HEAD origin/main` prints 3308996bb5c735ad86a963685cb0622404fcfd3c for both.
- **`DEP-015`** (NA): Precondition: does this row's notion of deployment map onto a macOS LaunchAgent? It does not hold: this is not an auto-deploying project (no deploy job in .github/workflows/ci.yml, no hosted service, LaunchAgent runs the working tree), and no server-side error investigation was in scope for this audit. The command was run anyway: `git rev-list --left-right --count origin/main...main` prints `0\t0`, so there is zero lag in either direction.
- **`DEP-016`** (PASS): Precondition: does this row's notion of deployment map onto a macOS LaunchAgent? It holds: 'the deployed app' maps onto the panel served by the running LaunchAgent daemon at http://127.0.0.1:7420/panel, so the rule applies. Verified: `curl -D -` on /panel returns 200 text/html from the live daemon, and lib/server.js:552 plus serveFile at :563-567 read web/panel.html off disk per request, so what the deployed surface serves is current source rather than a stale bundle. No UI fix is claimed live anywhere in the tree; the 20 open UI findings in UX_FEEDBACK.md are each stated as measured against a real rendered panel (headless 640x900 renders and Chromium computed-style checks), not against a local mock.
- **`DEP-017`** (NA): Precondition: does this row's notion of deployment map onto a macOS LaunchAgent? It does not hold: the project has never had more than one user-facing URL, so there is no superseded service to suspend or note. grep for http(s) URLs in README.md returns exactly two, http://localhost:7420 (README.md:76, the canonical dashboard) and the GitHub clone URL (README.md:55). The only other port is the reader's internal CDP port 7421 (config/interstice.config.default.json reading.readerPort), which is a debugging socket for a headless Chrome, not a user-facing surface.
- **`DEP-018`** (NA): Precondition: does this row's notion of deployment map onto a macOS LaunchAgent? It does not hold for the deploy half: there is no Render service and therefore no deploy hook to wire, and .github/workflows/ci.yml already exists and predates any manual deploy (committed 2026-07-30, test-only). Reported for completeness since it is verifiable independently of deployment: `git remote -v` shows origin carrying one push URL (https://github.com/scott-lydon/interstice.git) and no GitLab mirror, so dual push is not configured on this repo.
- **`DF-005`** (NA): CONDITIONAL. Precondition: a Claude Design handoff bundle exists and docs/design-immersive-reading.html is the canonical string source. It does NOT hold: the repo's own selection manifest records "no Claude Design handoff bundle" (docs/GOAL_LOOP_STARS_READER_2026-08-17.md:693, docs/RECURRING_GOALS_SELECTION.md:61), and the artifact is a three-state visual mock with illustrative copy, not a string inventory, produced by the frontend-design/design-reference route prescribed at docs/GOAL_LOOP_STARS_READER_2026-08-17.md:371-373. Drift recorded as asked: the live panel does carry explanatory fallback strings the mock has no counterpart for ("This page did not read cleanly, so this is the page itself." web/panel.html:902; "· some of this page read poorly" web/panel.html:916; "Remembered from ... this position may have moved since" web/panel.html:1267-1269), but with no design bundle to diff against, those are judged by UFC-007 and UFC-009 above rather than as fidelity offences.
- **`DF-006`** (NA): CONDITIONAL. Same precondition as DF-005, and it does NOT hold: there is no Claude Design export in the tree (docs/GOAL_LOOP_STARS_READER_2026-08-17.md:693). Drift recorded as asked, and it runs live-away-from-design in both directions: the design shows a persistent hint "← → turn the page" (docs/design-immersive-reading.html:79) that the live panel never renders; the design's rung labels are "Flashcards / Reading / Queue next / To-do" (docs/design-immersive-reading.html:93) while the live header reads "Cards / Book / Queue / To-do" (web/panel.html:324-327); the design promises the page at >=90% of both dimensions (docs/design-immersive-reading.html:82) while #book-actions still renders under the page in immersive mode (measured display:flex, inline style at web/panel.html:424); and the design's star detail reads "Star 1  09:12 → 09:37 (25m unbroken)" (docs/design-immersive-reading.html:115) against the live "A block earned on <day>: began X · ended Y" (web/panel.html:1844). No case was resolved by editing the design to match the code — the design file has not been touched since it was written.
- **`VD-007`** (NA): CONDITIONAL. Precondition: a palette or design-token rename shipped (the batch records it as "applies only if phase 2 introduces a token layer"). It did NOT hold in this repo: git show over the phase 2-5 commits (d0613a1, d8ec9be, fdfb5bf, 9949966) shows zero added, removed or renamed CSS custom properties in web/panel.html, and the panel's :root block at web/panel.html:9-18 is unchanged since commit e7f55a7. The only rename in the period is a localStorage key, MODE_KEY = "interstice.readerMode.v2" (web/panel.html:765), which is not a design token.
