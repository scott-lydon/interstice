{"row_id": "TS-059", "verdict": "PASS", "severity": "blocker", "evidence": "grep for `eval(`, `new Function`, and string-argument `setTimeout(`/`setInterval(` across lib/, bin/ and hooks/ returns zero hits. Every timer takes a function: lib/engine.js:28 `setTimer = (fn, ms) => setTimeout(fn, ms)`, lib/daemon.js:95 `setInterval(() => this.#heartbeat(), ...)`.", "fix": ""}
{"row_id": "TS-060", "verdict": "PASS", "severity": "medium", "evidence": "The target files use only standardized syntax: ESM (package.json:5 `\"type\": \"module\"`), class `#private` fields (lib/daemon.js:44 `#attach`, lib/engine.js:77 `#log`), `??`/`?.`, and standard runtime APIs (`AbortSignal.timeout` lib/doctor.js:199, `randomUUID` lib/engine.js:2). No vendor-prefixed or proposal-stage constructs appear.", "fix": ""}
{"row_id": "TS-061", "verdict": "PASS", "severity": "blocker", "evidence": "grep for `.prototype`, `globalThis`, `window.`, and `Object.assign` onto builtins across lib/, bin/ and hooks/ returns zero hits. No module in the batch locality writes to a builtin or to the global scope.", "fix": ""}
{"row_id": "TS-062", "verdict": "FAIL", "severity": "high", "evidence": "Casing per kind is correct (classes UpperCamelCase, functions lowerCamelCase, module constants CONSTANT_CASE, all ASCII), but the rule's single-character clause is broken repeatedly by ordinary locals that are neither loop indices nor callback parameters: lib/panel.js:56 `const f = JSON.parse(stdout);`, lib/panel.js:77-78 `const w = Math.min(...)` / `const h = Math.min(...)`, lib/panel.js:192 `const s = this.settings;`, lib/config.js:47 `const q = cfg.quietHours;`, lib/server.js:130 `const s = daemon.panel.state();`, lib/doctor.js:303 `const f = await screenFrame();`, lib/doctor.js:368 `const d = readingRungDiagnosis({...});`, bin/interstice.js:116 `const h = await api('/api/health');`.", "fix": "Rename the single-character locals to words: `f`->`frame`, `w`/`h`->`clampedWidth`/`clampedHeight`, `s`->`settings` or `panelState`, `q`->`quietHours`, `d`->`diagnosis`, `h`->`health`. Leave loop indices (`for (let i = ...)` lib/router.js:68) and short callback params (`(l) =>` lib/panel.js:178) alone."}
{"row_id": "TS-063", "verdict": "FAIL", "severity": "medium", "evidence": "Names are otherwise domain-clear, but letters are deleted inside words in several identifiers: bin/interstice.js:55 `function fmtSec(s)` (`fmt` for `format`, plus the single-char parameter `s`), lib/config.js:19 `export function validate(cfg)` (`cfg`), lib/daemon.js:45 `source.on('submit', (ev) =>` (`ev`), lib/doctor.js:236 `const ep = anki.endpoint(config);` (`ep`). The single-letter locals listed under TS-062 also fall under this rule's \"single-letter names outside the allowed cases\" clause.", "fix": "Rename `fmtSec`->`formatSeconds`, its parameter `s`->`seconds`, `cfg`->`config`, `ev`->`event`, `ep`->`endpoint`."}
{"row_id": "TS-064", "verdict": "PASS", "severity": "medium", "evidence": "No project identifier carries two or more consecutive capitals. The codebase consistently treats acronyms as words: lib/server.js:188 `cloudReaderUrl`, lib/state/system.js import `openUrl` at bin/interstice.js:11. The only multi-capital identifiers are platform-mandated: `randomUUID` (lib/engine.js:2), `toISOString` (lib/engine.js:89), `AbortSignal` (lib/doctor.js:199).", "fix": ""}
{"row_id": "TS-065", "verdict": "PASS", "severity": "medium", "evidence": "grep for leading or trailing underscores on identifiers across lib/server.js, lib/daemon.js, lib/panel.js, lib/router.js, lib/engine.js, lib/config.js, lib/doctor.js and bin/interstice.js returns zero. Privacy is expressed with real `#private` fields (lib/engine.js:77 `#log`, lib/panel.js:190 `#spawn`), and the one underscore is the allowed unused-parameter form at lib/doctor.js:224 `ANKI_BUNDLES.filter((_, i) => !states[i])`.", "fix": ""}
{"row_id": "TS-067", "verdict": "PASS", "severity": "low", "evidence": "Every alias is an import binding (therefore const) and is clearly derived from the original: lib/engine.js:3 `import { choose, advance as advanceRung }`, lib/server.js:7 `reconnect as ankiReconnect`, lib/daemon.js:22 `import { open as openStarStore }`. No `let` aliases exist in the batch locality.", "fix": ""}
{"row_id": "TS-068", "verdict": "NA", "severity": "medium", "evidence": "The batch locality is plain ES-module JavaScript (package.json:5 `\"type\": \"module\"`, no tsconfig.json, no .ts files). There are no type annotations at all, so there is neither a redundant local annotation nor a missing exported return annotation to evaluate.", "fix": ""}
{"row_id": "TS-069", "verdict": "NA", "severity": "medium", "evidence": "No type aliases exist anywhere in the batch locality: the tree is plain JavaScript with no `type` declarations, so there is no `type MaybeFoo = Foo | null` construct to flag.", "fix": ""}
{"row_id": "TS-070", "verdict": "NA", "severity": "medium", "evidence": "No interfaces or annotated parameters exist in the batch locality (plain JavaScript, no tsconfig.json), so there is no `: T | undefined` field or parameter to compare against the `?:` form. Optionality is expressed with destructuring defaults instead, e.g. lib/panel.js:322 `async answersTo(pid, { graceMs = 4500, pollMs = 300 } = {})`.", "fix": ""}
{"row_id": "TS-071", "verdict": "NA", "severity": "low", "evidence": "There are no type declarations of any kind in the batch locality, therefore no nominal brand fields and no phantom type parameters to flag.", "fix": ""}
{"row_id": "TS-072", "verdict": "NA", "severity": "low", "evidence": "No array type annotations exist (plain JavaScript). Arrays are plain values, e.g. lib/router.js:15 `export const RUNGS = ['flashcards', 'reading', 'queue_prompt', 'todo'];`, so neither the `T[]` nor the `Array<T>` form is present to judge.", "fix": ""}
{"row_id": "TS-073", "verdict": "NA", "severity": "medium", "evidence": "No index signature types exist (plain JavaScript). The one dynamic-key object read, lib/server.js:545 `if (routes[key]) return await routes[key](req, res, url);`, is already guarded by a presence check, so the runtime hazard the rule targets does not occur here either.", "fix": ""}
{"row_id": "TS-074", "verdict": "NA", "severity": "low", "evidence": "No mapped or conditional types exist in the batch locality; the tree is plain JavaScript with no type-level programming.", "fix": ""}
{"row_id": "TS-075", "verdict": "NA", "severity": "high", "evidence": "The `any` type has no referent here: the batch locality is plain ES-module JavaScript with no type annotations and no tsconfig.json, and grep for `: any`, `{any}` and `{*}` across lib/ and bin/ returns zero hits. There is also no implicit-any surface, because there is no compiler configured to infer one.", "fix": ""}
{"row_id": "TS-076", "verdict": "NA", "severity": "medium", "evidence": "No `: {}` annotation and no `extends {}` constraint exists; every `{}` in the batch locality is a value-position empty object literal or a destructuring default, e.g. lib/config.js:57 `export function load({ force = false } = {})` and lib/server.js:79 `let companionOverrides = {};`.", "fix": ""}
{"row_id": "TS-077", "verdict": "NA", "severity": "low", "evidence": "No tuple types exist in the batch locality (plain JavaScript). Array destructuring is used at lib/config.js:15 `for (const [k, v] of Object.entries(over))` and lib/engine.js:139, but no positional type is declared.", "fix": ""}
{"row_id": "TS-078", "verdict": "PASS", "severity": "medium", "evidence": "The only type positions in the batch locality are JSDoc types, and they use the lowercase forms: lib/router.js:75-80 `@param {object} input`, `@param {number} input.elapsed`, `@returns {{action:'deliver'|'hold', rung?:string, reason:string}}`. grep for `{String}`, `{Number}`, `{Boolean}`, `{Object}`, `{Symbol}` returns zero. Capitalized `String(...)`/`Number(...)` calls (lib/server.js:146, 166) are value-position conversions, not type positions.", "fix": ""}
{"row_id": "TS-079", "verdict": "NA", "severity": "high", "evidence": "No generic functions and no type parameters exist in the batch locality (plain JavaScript, no tsconfig.json), so there is no return-type-only type parameter to flag.", "fix": ""}
{"row_id": "TS-080", "verdict": "PASS", "severity": "high", "evidence": "grep for `@ts-ignore` and `@ts-expect-error` across lib/, bin/ and hooks/ returns zero hits; there is also no `// @ts-check` pragma that would make such suppressions meaningful. No suppression is present to be undocumented.", "fix": ""}
{"row_id": "TS-081", "verdict": "FAIL", "severity": "low", "evidence": "JSDoc is used for internal implementation notes attached to no exported symbol. lib/panel.js:87-92 is an orphaned JSDoc block (`/** The book used to open in a window of its own, and the way back from it was a bookmarks bar seeded into this profile. Neither exists now ... See lib/reader.js. */`) sitting between two unrelated declarations and documenting nothing. Similarly lib/engine.js:200-208 puts a pure implementation note (`An idle veto is transient ... Deliberately NOT done for wrong_app.`) in JSDoc on the private `#scheduleRetry`.", "fix": "Convert lib/panel.js:87-92 and lib/engine.js:200-208 to stacked `//` line comments, since neither documents a consumer-facing API symbol. Reserve `/** */` for the exported surface."}
{"row_id": "TS-082", "verdict": "FAIL", "severity": "high", "evidence": "Ten top-level exports in the batch locality carry no JSDoc: lib/config.js:6 `export function defaults()`, :19 `export function validate(cfg)`, :57 `export function load(...)`, :77 `export function writeUserConfig(...)`; lib/router.js:15 `export const RUNGS` and :17 `export const DECLINE`; lib/server.js:71 `export async function createServer({ daemon, config })`; lib/panel.js:106 `export class Panel`; lib/daemon.js:26 `export class Daemon`; lib/doctor.js:118 `export async function runDoctor({ fix = false } = {})`. For Daemon, Panel, createServer and runDoctor a descriptive block exists in the file but is detached from the declaration by intervening statements (lib/daemon.js:15-21 is followed by imports on lines 22-24; lib/server.js:16-24 is followed by `const json` on line 26; lib/doctor.js:77-91 is followed by `const PASS` on line 93).", "fix": "Add a JSDoc block immediately above each of the ten exports. For the four detached blocks, move lib/daemon.js:15-21 directly above `export class Daemon`, lib/server.js:16-24 directly above `export async function createServer`, lib/doctor.js:77-91 directly above `export async function runDoctor`, and lib/panel.js:9-24 directly above `export class Panel` (leaving any genuinely file-level prose as a separate `@fileoverview` block at the top)."}
{"row_id": "TS-083", "verdict": "NA", "severity": "medium", "evidence": "The rule governs JSDoc types in TypeScript files; there are no .ts/.tsx files in the repo and no tsconfig.json. The brace-delimited types that do appear (lib/router.js:75-80 `@param {object} input`) are in plain JavaScript, where JSDoc is the only place a type can be written, so the duplication the rule guards against cannot occur.", "fix": ""}
{"row_id": "TS-084", "verdict": "PASS", "severity": "medium", "evidence": "Comments consistently carry information the code cannot, and none restate the adjacent statement. Examples: lib/panel.js:311-320 explains why a live pid is not proof of a live window (`a Chrome that has lost its window does not always exit`), lib/router.js:44-46 explains why null is unavailable rather than empty, hooks/on-submit.sh:13-15 records why `date +%s%3N` silently produced invalid JSON. The literal-argument half of this rule's detection is reported under TS-113 rather than duplicated here.", "fix": ""}
{"row_id": "TS-085", "verdict": "PASS", "severity": "high", "evidence": "`git status --short` shows no modification to any file in the batch locality (only docs/ paths and untracked files are dirty), so there is no in-flight diff carrying a reformatting hunk. The committed files are internally consistent: two-space indent, single quotes, semicolons and ESM throughout lib/server.js, lib/daemon.js, lib/panel.js, lib/router.js, lib/engine.js, lib/config.js, lib/doctor.js and bin/interstice.js.", "fix": ""}
{"row_id": "TS-086", "verdict": "NA", "severity": "low", "evidence": "grep for `@deprecated` and for the word `deprecat` across lib/, bin/ and hooks/ returns zero hits, and no symbol is described as superseded in prose. lib/panel.js:87-92 describes behaviour that was removed outright rather than a symbol still present but deprecated, so there is no deprecation construct to evaluate.", "fix": ""}
{"row_id": "TS-087", "verdict": "NA", "severity": "high", "evidence": "There is no generated code in the batch locality: grep for `@generated`, `GENERATED` and `Code generated` across lib/, bin/ and web/ returns zero, package.json:11-15 declares no build or codegen script, and every file in the locality is hand-authored source.", "fix": ""}
{"row_id": "TS-088", "verdict": "PASS", "severity": "low", "evidence": "No license or copyright text appears in any line comment or below the imports in the batch locality; licensing is carried at repository level by /Users/scottlydon/Developer/interstice/LICENSE and package.json:6 `\"license\": \"MIT\"`. There is therefore no misplaced license text to flag.", "fix": ""}
{"row_id": "TS-089", "verdict": "FAIL", "severity": "medium", "evidence": "The `import type` half of this rule has no referent in plain JavaScript, but the unused-binding clause is violated: lib/doctor.js:18 `import { COWORK_SESSIONS_ROOT, CLAUDE_CODE_PROJECTS, CLAUDE_SETTINGS, ROOT, LAUNCH_AGENT, LOG_DIR } from './paths.js';` pulls in three bindings the file never references. grep confirms `CLAUDE_CODE_PROJECTS`, `ROOT` and `LOG_DIR` each occur exactly once in lib/doctor.js, on the import line itself.", "fix": "Trim lib/doctor.js:18 to `import { COWORK_SESSIONS_ROOT, CLAUDE_SETTINGS, LAUNCH_AGENT } from './paths.js';`."}
{"row_id": "TS-090", "verdict": "NA", "severity": "medium", "evidence": "There are no type re-exports in the batch locality: every `export` in lib/config.js, lib/router.js, lib/panel.js, lib/engine.js, lib/daemon.js, lib/server.js and lib/doctor.js is a runtime value (function, class or const), and no file re-exports a symbol from another module. No design note claims `export type` guarantees anything.", "fix": ""}
{"row_id": "TS-091", "verdict": "PASS", "severity": "low", "evidence": "A scripted scan of the eight target files found no blank line immediately after a function-body opening brace and none immediately before a closing brace. grep for `function *`, `yield *` and a space after the spread ellipsis (`... `) returns zero hits; spread is written tight, e.g. lib/engine.js:73 `counters: { ...this.counters }` and lib/server.js:151 `{ ok: true, ...(await daemon.panel.show(...)) }`.", "fix": ""}
{"row_id": "TS-092", "verdict": "PASS", "severity": "high", "evidence": "Every `this` in the batch locality is inside a class constructor, a class method, or an arrow function lexically inside one: lib/engine.js:36-54 (constructor), lib/panel.js:341-347 (`async show`), lib/daemon.js:45-49 (arrow inside `#attach`). grep for `this` outside a `this.` member access returns only prose inside comments, and grep for `.call(` / `.apply(` returns zero hits, so `this` is never rebound or used as a global.", "fix": ""}
{"row_id": "TS-093", "verdict": "NA", "severity": "low", "evidence": "Return type annotations do not exist as a construct here: the batch locality is plain JavaScript with no tsconfig.json, and the project states no annotation policy in CLAUDE.md or package.json. The one genuinely opaque return is already documented in JSDoc at lib/router.js:80 `@returns {{action:'deliver'|'hold', rung?:string, reason:string}}`.", "fix": ""}
{"row_id": "TS-094", "verdict": "FAIL", "severity": "medium", "evidence": "Absence is carried by both sentinels for the same concept. lib/router.js:36-38 has to accept either for one value: `const due = state.ankiDue; if (due === null || due === undefined) return false; // unavailable`. Inside a single response handler, lib/server.js:235-236 uses undefined for absent request fields (`width: Number(body.width) || undefined,` / `height: Number(body.height) || undefined,`) while lib/server.js:263-264 uses null for absent response fields (`title: state.book?.title ?? null,` / `percent: state.book?.percent ?? null,`).", "fix": "Pick one sentinel per concept and enforce it at the producer. Since lib/state/index.js:55-65 already yields `null` for an unreadable `ankiDue`, simplify lib/router.js:37 to `if (due === null) return false;`, and normalise lib/server.js:235-236 to `?? null` (or document in the module JSDoc that request-side omission is undefined and response-side absence is null)."}
{"row_id": "TS-095", "verdict": "NA", "severity": "high", "evidence": "There is no `any` to replace: the batch locality is plain ES-module JavaScript with no type annotations, and grep for `: any`, `{any}` and `{*}` across lib/ and bin/ returns zero hits.", "fix": ""}
{"row_id": "TS-096", "verdict": "NA", "severity": "high", "evidence": "Neither `any` nor `unknown` appears anywhere in the batch locality; the tree is plain JavaScript with no type annotations, so there is no not-known type to move from `any` to `unknown` and no unnarrowed `unknown` dereference.", "fix": ""}
{"row_id": "TS-097", "verdict": "NA", "severity": "medium", "evidence": "No `any` exists in the batch locality and no lint-suppression comment of any kind appears (grep for `eslint-disable` and `any` type annotations across lib/ and bin/ returns zero), so there is neither an unexplained suppression nor an unsuppressed legitimate `any` to evaluate.", "fix": ""}
{"row_id": "TS-098", "verdict": "PASS", "severity": "low", "evidence": "No identifier in the batch locality contains a dollar sign. Every `$` occurrence is template-literal interpolation, e.g. lib/panel.js:142 `return \\`http://127.0.0.1:${this.config.port}${route}\\`;` and lib/server.js:542 `const key = \\`${req.method} ${url.pathname}\\`;`.", "fix": ""}
{"row_id": "TS-099", "verdict": "NA", "severity": "low", "evidence": "No type parameters exist in the batch locality; the tree is plain JavaScript with no generics, so there is no type-parameter naming to judge.", "fix": ""}
{"row_id": "TS-100", "verdict": "NA", "severity": "low", "evidence": "This batch's locality (lib/server.js, lib/daemon.js, lib/panel.js, lib/router.js, lib/engine.js, lib/config.js, lib/doctor.js, bin/interstice.js, hooks/*.sh) contains no test files; the suite lives under test/ and is out of scope for this batch.", "fix": ""}
{"row_id": "TS-102", "verdict": "NA", "severity": "blocker", "evidence": "There are no TypeScript files to type check: the repository has no tsconfig.json or jsconfig.json (both confirmed absent), no .ts/.tsx sources, no `// @ts-check` pragma anywhere in lib/ or bin/, and package.json:12 defines the test script as `node --test` with no compile step. The rule's subject is therefore absent rather than violated, though it is worth recording that the project runs with no static type checking at all.", "fix": ""}
{"row_id": "TS-103", "verdict": "PASS", "severity": "blocker", "evidence": "grep across lib/, bin/ and hooks/ for `eval(`, `new Function`, `innerHTML`, `outerHTML`, `document.write`, `dangerouslySetInnerHTML` and `globalThis` returns zero hits. Server responses are built with `JSON.stringify` and explicit content types (lib/server.js:26-34), static files are served as bytes with a fixed type (lib/server.js:563-568), and the listener is loopback-bound at lib/server.js:573 `server.listen(config.port, '127.0.0.1', resolve);`.", "fix": ""}
{"row_id": "TS-104", "verdict": "FAIL", "severity": "medium", "evidence": "Later additions did not follow what the file already does. lib/server.js opens with a grouped import block on lines 1-14, then drops a stray import fifty lines into the file at line 69: `import { starsHandler } from './focus/stars-routes.js';`. lib/daemon.js does the same, splitting its import block (lines 1-13) from three more imports placed after the class JSDoc at lines 22-24: `import { open as openStarStore } from './focus/store.js';`, `import { LOG_DIR } from './paths.js';`, `import path from 'node:path';` -- which also detaches that JSDoc from `export class Daemon` on line 26.", "fix": "Move lib/server.js:69 into the top import block (after line 14), and move lib/daemon.js:22-24 into the top import block (after line 13) so the JSDoc on lines 15-21 sits directly above `export class Daemon`."}
{"row_id": "TS-105", "verdict": "FAIL", "severity": "low", "evidence": "Non-JSDoc block comments are used where line comments are required: lib/daemon.js:162 `/* already gone */`, lib/panel.js:225 and lib/panel.js:297 `/* raising is a convenience; losing it must not stop the delivery */`. Two comments are also drawn as dash-ruled boxes: lib/server.js:121 `// ---- the panel: one window, and everything the rungs put in it ----` and lib/server.js:495 `// ---- debug: drive the machine into states the happy path cannot reach ----`.", "fix": "Rewrite the three `/* ... */` inline comments as `//` line comments inside their catch blocks, and strip the dash rules from lib/server.js:121 and :495 so they read as plain `// the panel: ...` and `// debug: ...`."}
{"row_id": "TS-106", "verdict": "PASS", "severity": "low", "evidence": "Every documentation block in the batch locality opens with `/**` and closes with `*/` on its own line, with each interior line prefixed by ` * `. Examples: lib/router.js:28-32, lib/panel.js:44-49, lib/server.js:53-60, lib/engine.js:10-22. No single-asterisk block masquerades as JSDoc and no block is left unterminated (all eight files parse and run under `node --test`).", "fix": ""}
{"row_id": "TS-107", "verdict": "PASS", "severity": "low", "evidence": "The two JSDoc blocks containing lists both use real Markdown ordered lists with aligned continuations that survive rendering: lib/router.js:9-12 (`*   1. It never returns a list. ...` / `*   2. It never returns a rung with no work in it. ...`) and lib/doctor.js:83-88 (`*   1. macOS App Nap suspends Anki ...` / `*   2. The Cowork transcript path changes ...`). No hand-drawn table or layout indentation appears in any JSDoc body in the locality.", "fix": ""}
{"row_id": "TS-108", "verdict": "PASS", "severity": "low", "evidence": "The only block tags in the batch locality are in lib/router.js:75-80, and each sits alone at the start of its line: `@param {object} input`, `@param {number} input.elapsed`, `@param {object} input.state`, `@param {object} input.config`, `@returns {{action:'deliver'|'hold', rung?:string, reason:string}}`. No tag shares a line with prose or with another tag, and both tags used are within the allowed subset.", "fix": ""}
{"row_id": "TS-109", "verdict": "FAIL", "severity": "low", "evidence": "The one wrapped block tag in the batch locality uses discouraged horizontal alignment rather than a four-space indent. lib/router.js:77-78: `* @param {object} input.state     live world: ankiDue, bookInProgress, idleMs,` continued on the next line as `*                                 frontmostApp, current, stoodDown, cooldownUntil, now` -- the continuation is padded out to line up under the description column.", "fix": "Reflow lib/router.js:76-79 so each tag's description starts one space after the tag and any wrap is indented exactly four spaces from the `*`, dropping the column alignment on lines 76-79."}
{"row_id": "TS-110", "verdict": "FAIL", "severity": "medium", "evidence": "Two of the three classes in the locality have no JSDoc attached. `export class Panel` at lib/panel.js:106 carries none; the block that describes it (lib/panel.js:9-24) sits above `const BROWSERS` on line 26 instead. `export class Daemon` at lib/daemon.js:26 also carries none; its describing block (lib/daemon.js:15-21) is separated from the declaration by the imports on lines 22-24. Only `export class Engine` (lib/engine.js:10-23) has an attached block, and it does say when to reach for the class.", "fix": "Move lib/panel.js:9-24 to sit immediately above `export class Panel` on line 106, and move lib/daemon.js:22-24 (the stray imports) up into the top import block so lib/daemon.js:15-21 lands directly above `export class Daemon`."}
{"row_id": "TS-111", "verdict": "FAIL", "severity": "low", "evidence": "Several method descriptions are written as imperatives rather than third-person verb phrases: lib/panel.js:236 `* Bring the panel forward without disturbing anything else.`, lib/panel.js:266 `* Make sure there is exactly one window, with only one attempt in flight.`, lib/panel.js:334 `* Put a rung on screen.`, and lib/doctor.js:492 `* Prove the detection mechanism itself, in the shape that actually matters: ...`.", "fix": "Rewrite as third person: \"Brings the panel forward ...\", \"Makes sure there is exactly one window ...\", \"Puts a rung on screen.\", \"Proves the detection mechanism itself ...\"."}
{"row_id": "TS-112", "verdict": "NA", "severity": "medium", "evidence": "Parameter properties are a TypeScript-only construct and none exist here. Every constructor in the locality destructures a plain options object and assigns explicitly, e.g. lib/panel.js:107-122 `constructor({ config, logger } = {}) { this.config = config; ... }` and lib/engine.js:24-55.", "fix": ""}
{"row_id": "TS-113", "verdict": "FAIL", "severity": "medium", "evidence": "Bare numeric and null arguments appear at call sites with no parameter-name comment. lib/config.js:26-31 calls `num('arm', 1, 3600);`, `num('mid', 1, 7200);`, `num('long', 1, 14400);`, `num('cooldown', 0, 3600);`, `num('idleVetoMs', 0, 120000);`, `num('port', 1024, 65535);` -- three-argument call sites where the guide asks for the interface-plus-destructure refactor. Also lib/doctor.js:150 `countTranscripts(COWORK_SESSIONS_ROOT, 1)` and :152 `countTranscripts(COWORK_SESSIONS_ROOT, 5000)`, bin/interstice.js:47 `process.kill(pid, 0);`, and lib/server.js:151 `daemon.panel.show(rung, body.detail ?? null)`.", "fix": "Refactor lib/config.js:21-31 so `num` takes an options object (`num({ key: 'arm', min: 1, max: 3600 })`). Add parameter-name comments at the remaining sites: `countTranscripts(COWORK_SESSIONS_ROOT, /* limit= */ 1)`, `process.kill(pid, /* signal= */ 0)`, `daemon.panel.show(rung, /* detail= */ body.detail ?? null)`. lib/panel.js:99 already models this with its trailing `// throws if it is gone`."}
{"row_id": "TS-114", "verdict": "NA", "severity": "medium", "evidence": "No decorators exist in the batch locality: grep for a line beginning with `@` followed by an uppercase letter across lib/server.js, lib/daemon.js, lib/panel.js, lib/router.js, lib/engine.js, lib/config.js, lib/doctor.js and bin/interstice.js returns zero, and there is no decorator-enabling tsconfig.json. There is therefore no JSDoc that could sit between a decorator and its declaration.", "fix": ""}
{"row_id": "TS-PP-001", "verdict": "NA", "severity": "high", "evidence": "There is no Zustand store, no equivalent store, and no React at all in the batch locality: grep for `zustand`, `useStore`, `useEffect` and `useState` across lib/ and bin/ returns zero hits, and package.json:28-30 lists only `@playwright/test` as a dependency. The panel and dashboard are plain HTML under web/, outside this batch.", "fix": ""}
{"row_id": "TS-PP-002", "verdict": "FAIL", "severity": "high", "evidence": "Modules reach past their neighbours' entry points and into their internals. lib/server.js:11 imports the state facade `import { invalidate, readingSnapshot } from './state/index.js';` yet lines 7-9 and 13 bypass it to import four sibling implementation modules directly (`./state/anki.js`, `./state/kindle.js`, `./state/notes.js`, `./state/companions.js`). lib/doctor.js:383 reaches two levels into another module's implementation folder with `const { screenLocked } = await import('./focus/breakers/display.js');`. Route handlers also read another object's internal fields rather than calling a contract: lib/server.js:222 `if (daemon.reader.signingIn)`, :264 `seq: daemon.reader.seq`, :509 `if (!daemon.engine.gap) return json(res, 409, ...)`.", "fix": "Re-export the anki/kindle/notes/companions surface that lib/server.js needs from lib/state/index.js and import only from that facade; export `screenLocked` from a lib/focus entry module instead of importing lib/focus/breakers/display.js directly; and replace the internal field reads with accessors on the owning classes (e.g. `daemon.reader.signInStatus()` already exists, and add `daemon.engine.hasOpenGap()` for lib/server.js:509 and a reader status method exposing `seq` and `signingIn` together)."}
{"row_id": "TS-066", "verdict": "FAIL", "severity": "medium", "evidence": "Six CONSTANT_CASE module-level bindings hold mutable objects that are never frozen: lib/config.js:4 `const KNOWN_RUNGS = new Set(['flashcards', 'reading', 'queue_prompt', 'todo']);` (a Set is mutable), lib/router.js:15 `export const RUNGS = ['flashcards', 'reading', 'queue_prompt', 'todo'];`, lib/router.js:17 `export const DECLINE = { BELOW_THRESHOLD: 'below_threshold', ... };`, lib/panel.js:26 `const BROWSERS = [{ app: 'Google Chrome', bin: '...' }, ...];`, lib/doctor.js:72 `const ANKI_BUNDLES = ['net.ankiweb.dtop', 'net.ankiweb.launcher'];`, lib/doctor.js:75 `const PANEL_BROWSERS = ['Google Chrome', 'Brave Browser', 'Microsoft Edge', 'Chromium'];`. The CONSTANT_CASE string bindings in the same files (lib/doctor.js:93-95 `PASS`/`FAIL`/`WARN`, lib/panel.js:33 `FRAME_SCRIPT`, bin/interstice.js:17 `USAGE`) are deeply immutable and correctly named. lib/config.js:73 shows the project already knows the freeze idiom: `cached = Object.freeze(cfg);`.", "fix": "Deep-freeze the six mutable constants so their CONSTANT_CASE names are truthful, e.g. `export const RUNGS = Object.freeze([...])`, `export const DECLINE = Object.freeze({...})`, `const BROWSERS = Object.freeze([...].map(Object.freeze))`, `const ANKI_BUNDLES = Object.freeze([...])`, `const PANEL_BROWSERS = Object.freeze([...])`; for lib/config.js:4 either keep the Set and rename it `knownRungs`, or freeze a backing array."}
{"row_id": "TS-101", "verdict": "PASS", "severity": "low", "evidence": "The batch locality contains exactly one namespace import and its alias is lowerCamelCase: lib/doctor.js:6 `import * as anki from './state/anki.js';`, used as `anki.endpoint(config)` (lib/doctor.js:235), `anki.version(config)` (:245) and `anki.dueCount(config)` (:269). No snake_case or UpperCamelCase namespace alias exists, and neither jquery nor threejs is present to need the exemption.", "fix": ""}
