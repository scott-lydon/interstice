{"row_id": "UC-CUPID-001", "verdict": "FAIL", "severity": "high", "evidence": "lib/reader.js:314 `class Reader` exposes ~30 public methods (startSignIn, carry, reauthenticate, clearSiteData, retryBook, ensure, settle, capture, text, click, key, type, turn, shelf, closeIfIdle...), and lib/server.js reaches through that wide surface 44 times, including raw fields (lib/server.js:229 `daemon.reader.seq`, lib/server.js:298 `daemon.reader.running`, lib/server.js:222 `daemon.reader.signingIn`). By contrast lib/focus/store.js:44-48 shows the intended small surface.", "fix": "Split Reader into a session/auth unit, a page-capture unit, and an input unit, and give lib/server.js a narrow reading facade (e.g. `daemon.reading.view()`, `.input(kind, payload)`) so the HTTP layer never names Reader fields."}
{"row_id": "UC-CUPID-002", "verdict": "FAIL", "severity": "high", "evidence": "lib/reader.js:314 `class Reader` (file is 1722 lines) does browser process lifecycle (#start:744, close:1685) AND Amazon sign-in (startSignIn:430, waitUntilSignedIn:621) AND session carrying (carry:651, reauthenticate:697) AND viewport fitting (#applyViewport:931) AND screenshot capture with a prefetch cache (#shoot:1205, #prefetch:1473, #trim:1516) AND OCR text (text:1302) AND input injection (click:1540, key:1552, type:1576, turn:1596) AND shelf listing (shelf:1530). lib/server.js:1-582 likewise routes HTTP and owns reader/panel/companion orchestration inline.", "fix": "Split Reader along the 'and' boundaries above into ReaderSession (launch/auth/carry), ReaderPage (viewport/capture/prefetch/text) and ReaderInput (click/key/type/turn), each stated in one sentence."}
{"row_id": "UC-CUPID-003", "verdict": "FAIL", "severity": "high", "evidence": "lib/config.js:55 `let cached = null;` plus lib/config.js:57-58 `load({force=false})` returns the first-loaded config forever, so a second `load()` after the config file changes silently returns stale values with nothing in the name saying so. lib/state/companions.js:300 `let cache = null;` plus companionsSnapshot at lib/state/companions.js:306-311 returns a TTL-aged cached snapshot, so the same call with the same arguments returns different data depending on when and in what order it was called.", "fix": "Make the cache an explicit parameter or an injected object (e.g. `load(store)` / `companionsSnapshot(config, {cache})`) so a caller can see the ambient state it reads, and rename to `loadCached()` where memoization is intended."}
{"row_id": "UC-CUPID-004", "verdict": "PASS", "severity": "high", "evidence": "Every module is ESM with `node:`-prefixed builtins (lib/focus/store.js:9-11), uses `#private` fields (lib/reader.js:491 `#runSignIn`), and every test uses node:test with node:assert/strict. No transliterated construct, no `var`, and no pattern used by only one file was found across lib/, bin/, web/ and test/.", "fix": ""}
{"row_id": "UC-CUPID-005", "verdict": "PASS", "severity": "high", "evidence": "Module and type names come from the domain, not the framework: lib/focus/blocks.js (block), lib/focus/store.js (star), lib/focus/breakers/*.js (breaker), lib/actuators/index.js (actuator), lib/router.js (rung/ladder), lib/stats.js (gap). A grep for `class *(Manager|Helper|Util|Service|Handler|Data|Processor)` across lib/ and bin/ returns zero hits.", "fix": ""}
{"row_id": "UC-PRIN-001", "verdict": "FAIL", "severity": "high", "evidence": "The focus defaults have three representations each: lib/focus/breakers/frontmost.js:18 `DEFAULT_BLACKLIST = ['Slack','Discord',...]` vs config/interstice.config.default.json:97 `blacklistApps` vs README.md:408; lib/focus/breakers/video.js:33 `breakAfterMs = 4000` vs config/interstice.config.default.json:112 `videoBreakAfterMs: 4000` vs README.md:412; lib/focus/blocks.js:74 `blockMinutes = 25` vs config/interstice.config.default.json:96 vs README.md:407.", "fix": "Delete the in-code default literals and require the caller to pass the value read from config/interstice.config.default.json, then generate the README's default column from that file."}
{"row_id": "UC-PRIN-002", "verdict": "FAIL", "severity": "high", "evidence": "No current caller exists for the entire focus/latency extension surface: `createMachine` (lib/focus/blocks.js:74), `createFrontmostBreaker` (lib/focus/breakers/frontmost.js:41), `createDisplayBreaker` (lib/focus/breakers/display.js:38), `createVideoBreaker` (lib/focus/breakers/video.js:33), `probeVideo` (lib/video/probe.js:27), `createLatency` and `latencyEventFromEngine` (lib/latency.js:17,13) are referenced only from their own file and from test/. The daemon imports only the star store (lib/daemon.js:22,34) and never awards. `initialState` (lib/focus/blocks.js:28) has no caller at all.", "fix": "Either wire the breakers and machine into lib/daemon.js's tick so the config flags have a consumer, or delete the unreferenced factory functions and keep only the reducer the daemon actually calls."}
{"row_id": "UC-PRIN-003", "verdict": "FAIL", "severity": "high", "evidence": "lib/focus/blocks.js ships two public APIs for one job: the reducer trio `initialState` (:28), `reduce` (:38) and the stateful wrapper `createMachine` (:74). `initialState` has zero callers repo-wide and `reduce` is called only from inside createMachine (:78), so the simpler single API (createMachine alone, or the reducer alone) is correct and both are exported.", "fix": "Export only the API a caller uses: keep `reduce`+`initialState` internal to blocks.js and export `createMachine`, or drop the wrapper and let the daemon hold the state."}
{"row_id": "UC-PRIN-004", "verdict": "FAIL", "severity": "high", "evidence": "The panel size is stated in two places and they already disagree: README.md:339 `\"panel\": { \"width\": 440, \"height\": 620, ... }` against config/interstice.config.default.json:29-30 `\"width\": 640, \"height\": 900`. docs/STARS_NOTES_2026-08-19.md records the 440x620 -> 640x900 change as finished work, so the README copy is stale.", "fix": "Make config/interstice.config.default.json the single point of truth and generate the README configuration block from it, or replace the block with a pointer to the file."}
{"row_id": "UC-PRIN-005", "verdict": "FAIL", "severity": "high", "evidence": "Single responsibility is violated by lib/reader.js:314 `class Reader` (process lifecycle + auth + capture + OCR + input + shelf, ~30 methods) and interface segregation by lib/server.js, whose route handlers consume that whole class rather than a narrow reading protocol (lib/server.js:222-337, 44 `daemon.<unit>.<member>` reach-throughs). Open-closed and the protocols-over-polymorphism half are clean: the only `extends` in lib/ are Error subclasses (lib/focus/store.js:14 StarLogError, lib/video/whitelist.js:7 BadVideoURLError, lib/state/anki.js AnkiError), and the breakers are injected by function parameter rather than subclassed (lib/focus/breakers/display.js:38 `{ locked = screenLocked }`).", "fix": "Split Reader per UC-CUPID-002 and define a reading protocol (view/input/text/shelf) that lib/server.js depends on, so each route sees only the slice it needs."}
{"row_id": "UC-MOD-001", "verdict": "FAIL", "severity": "blocker", "evidence": "lib/server.js reaches through the daemon into three other modules' internals 44 times, including plain fields rather than contracts: lib/server.js:84 `daemon.engine.status`, :222 `daemon.reader.signingIn`, :229 `daemon.reader.seq`, :298 `daemon.reader.running`, :129 `daemon.panel.ping()`. Fixing a reading-surface bug therefore requires reading lib/server.js plus lib/daemon.js plus lib/reader.js (1722 lines) plus lib/panel.js, not one module and its direct contracts. The focus subtree is the counter-example done right (lib/focus/store.js, blocks.js and the breakers cross only plain data).", "fix": "Give the daemon a narrow reading/panel protocol (e.g. `daemon.reading.view()`, `daemon.reading.input()`, `daemon.reading.status()`) and forbid lib/server.js from naming any field of Reader or Panel, mirroring the contract style already used in docs/FOCUS_MODULE_CONTRACTS.md."}
{"row_id": "UC-ERR-001", "verdict": "FAIL", "severity": "blocker", "evidence": "Three failure paths produce no diagnosable error. lib/logger.js:23-25 `try { out.push(JSON.parse(line)) } catch { /* skip */ }` silently drops malformed lines of logs/gaps.jsonl, the file every dashboard figure is computed from, so a truncated gap disappears with no record of what failed or where. lib/server.js:46-48 `catch { resolve({}) }` turns a malformed request body into an empty body, so the handler reports a business error instead of a parse error. lib/doctor.js:270 `throw new Error('query failed')` names neither the query, the endpoint, nor the remedy, unlike the rest of doctor.js.", "fix": "In readJsonl, collect and return the 1-based line numbers that failed (the shape lib/focus/store.js:14-20 StarLogError already uses) and surface them on the dashboard; in readBody reject with a 400 naming the parse error and byte offset; give doctor.js:270 the query text and a remedy clause."}
{"row_id": "UC-ERR-002", "verdict": "FAIL", "severity": "blocker", "evidence": "Boundary catches that swallow, none of which is a long-lived loop boundary: lib/logger.js:23-25 (decode/persistence, malformed JSONL skipped silently), lib/server.js:45-49 (network decode, `resolve({})`), lib/state/kindle.js:56-62 `recallBooks()` `catch { return null }` which cannot tell a missing cache from a corrupt one, and lib/video/probe.js:12 `catch { return '' }` which turns an unparseable URL into an empty host that then flows into whitelist matching. The genuine loop boundaries do catch correctly (lib/watcher.js:78,87,116 and lib/daemon.js:161).", "fix": "At each of those four boundaries, distinguish the expected absence from the real fault: return a typed result (`{ok:false, reason}`) or throw a named error, and log the swallowed detail; keep bare catch only in the watcher/daemon loop bodies."}
{"row_id": "UC-CMT-001", "verdict": "FAIL", "severity": "high", "evidence": "lib/server.js:319-337, the `POST /api/reading/input` handler, sleeps `setTimeout(t, 350)` after a click (:324) and `setTimeout(t, 250)` after a key (:330) before forcing a capture, with no comment on why those two durations differ or where they came from. Every comparable pause elsewhere in the repo carries its reason (for example lib/companions-control.js:294-296 and lib/panel.js:302-303), so this is the one surprise left uncommented.", "fix": "State in a comment what those pauses are waiting for (the reader's repaint after an input) and where 350/250 were measured, or replace both with a wait on the reader's own seq/frame-change signal."}
{"row_id": "UC-CMT-002", "verdict": "FAIL", "severity": "high", "evidence": "The repo carries POSIX shell (hooks/on-submit.sh, hooks/on-stop.sh) with no named style guide and no spreadsheet under /Users/scottlydon/Developer/Recurring_goals/Code/ (that directory holds JavaScript_TypeScript_Development, Python_Development, React_NextJS_Development, Swift_Development, Testing_and_Coverage, Universal only). JavaScript, the primary language, has the spreadsheet but the repo names no guide anywhere: there is no eslint, prettier or editorconfig file and no mention of a guide in README.md, CLAUDE.md or package.json.", "fix": "Name the JavaScript guide in CLAUDE.md (Google JavaScript Style Guide is the popularity/excellence peer of google.github.io/swift) and add a Recurring_goals/Code/Shell_Development sheet adopting the Google Shell Style Guide for hooks/*.sh."}
{"row_id": "UC-DEAD-001", "verdict": "FAIL", "severity": "medium", "evidence": "Three exported symbols have zero references anywhere in the repository (searched lib, bin, web, test, hooks, .github, docs): lib/paths.js:33 `export const OFFSETS_FILE`, lib/amazon-session.js:36 `export function panelCookies`, lib/ocr.js:278 `export function toParagraphs`. None is listed as known dead code in docs/BUG_ISSUE_PREVENTION.md or either goal-loop file, so it is unreported rather than reported.", "fix": "Add a 'pre-existing dead code' list naming these three to docs/BUG_ISSUE_PREVENTION.md (report, do not delete, since no change in this audit orphaned them)."}
{"row_id": "UC-ENC-001", "verdict": "PASS", "severity": "high", "evidence": "No hand-rolled standardized encoder or decoder exists. Every encoding goes through a vetted implementation: `Buffer.from(..., 'base64')` at lib/companions-control.js:221 and lib/reader.js:1223, `toString('base64')` at lib/state/companions.js:279, and `encodeURIComponent` at lib/reader.js:45 and web/dashboard.html:325. No base58, JWT, CBOR or varint code is present.", "fix": ""}
{"row_id": "UC-NUM-001", "verdict": "FAIL", "severity": "medium", "evidence": "lib/server.js:324 `await new Promise((t) => setTimeout(t, 350));` and lib/server.js:330 `setTimeout(t, 250)` are bare numeric literals in a timing/threshold position inside the user-facing reading-input path, with no named constant and no comment stating where the numbers came from or why they differ.", "fix": "Hoist them to named constants (e.g. `CLICK_REPAINT_MS`, `KEY_REPAINT_MS`) with a comment recording the measurement they came from, or wait on the reader's frame-change signal instead of a fixed delay."}
{"row_id": "DI-001", "verdict": "PASS", "severity": "blocker", "evidence": "No mock, stub, placeholder or fixture data reaches a user-facing path: test/ contains only .test.js and .pw.mjs files with no fixture directory, and a repo-wide grep for lorem ipsum, example.com, dummy and placeholder finds only honest-empty-state copy (web/dashboard.html:271 'Nothing here is placeholder data: this stays empty until a real gap is logged.', web/panel.html:1834 'offline/file:// - honest empty, never stubbed'). The one synthetic source is the explicit /debug route, tagged `synthetic: true` at lib/server.js:502 and excluded from every figure by lib/stats.js:13,28.", "fix": ""}
{"row_id": "DI-002", "verdict": "PASS", "severity": "blocker", "evidence": "The work product stops and names what could not be produced rather than filling it in: docs/STARS_NOTES_2026-08-19.md:98-105 ('1.1 and 1.7 - the two that need a live cold-start') states that the failure log and the three cold-start proofs require a live Amazon-backed run and were not produced, and lines 24-31 state the same for the Phase 1 gate. I re-ran the suite this session (node --test, 275 pass / 5 fail; the 5 are the Playwright files, which cannot launch Chrome on this machine) and re-measured coverage, so the claims were checked against a fresh run rather than accepted.", "fix": ""}
{"row_id": "DI-003", "verdict": "PASS", "severity": "blocker", "evidence": "Every measured figure carries its date and method and I reproduced it fresh this session. docs/STARS_NOTES_2026-08-19.md:120 heads the coverage table 'measured 2026-08-19, node --experimental-test-coverage'; my own run of `node --test --experimental-test-coverage test/*.test.js` today returns lib/focus/store.js 100.00, lib/focus/blocks.js 94.85, lib/focus/stars-routes.js 95.65, lib/video/probe.js 96.55, lib/video/whitelist.js 100.00, lib/ocr.js 84.81, lib/panel.js 74.59 and lib/reader.js 61.67, matching the document line for line. docs/immersive-reading-measurements.md and docs/consistency-finding-2026-08-19.md are likewise dated records.", "fix": ""}
{"row_id": "DI-004", "verdict": "PASS", "severity": "blocker", "evidence": "Unmeasured numbers are null, not a fallback value. lib/state/kindle.js:217-218 `const progress = end > 0 ? Math.min(1, row.position / end) : null;` and `percent: progress === null ? null : ...` refuse to invent a reading percentage; lib/ocr.js:293 `const median = confidences.length ? ... : null;` and :309 `worst: confidences.length ? confidences[0] : null`; lib/state/anki.js:60-61 `catch { return null; // unavailable, not zero: the router treats these differently }`; lib/stats.js:30 filters non-finite durations out of the sample instead of coercing them.", "fix": ""}
{"row_id": "DI-005", "verdict": "PASS", "severity": "blocker", "evidence": "Aggregators exclude rather than coerce, and the display distinguishes the two states. lib/stats.js:30 `.filter((d) => Number.isFinite(d))` drops unmeasured durations before the percentile computation, and web/dashboard.html:209 renders the false-positive rate as `s.totals.delivered ? pct(fp) : '.'` so an unmeasured rate is a dot, never 0%. web/dashboard.html:216-224 additionally prints why the tiles read zero when everything logged was synthetic, and web/panel.html:894 gates the OCR-confidence warning on `d.confidence !== null` before consulting the weak fraction.", "fix": ""}
{"row_id": "DI-006", "verdict": "FAIL", "severity": "high", "evidence": "The scripted provider in this project is the /debug synthetic gap (lib/server.js:502 `synthetic: true`), and no contract test pins its aggregate: lib/stats.js, which computes every dashboard figure, is absent from the coverage report I generated this session (no test loads it) and no test in test/ imports `summarize`. test/engine.test.js:262 asserts only that the record carries the tag, not that a scan of synthetic gaps aggregates to zero. The injected stubs in the focus breakers (lib/focus/breakers/display.js:38 `locked`, frontmost.js:41 `frontmost`, video.js:33 `probe`) are likewise pinned only against themselves.", "fix": "Add test/stats.test.js asserting `summarize(onlySyntheticGaps).totals.gaps === 0` and `.totals.minutesReclaimed === 0` while the same gaps still render in the table, which is the 'total billable work aggregates to exactly zero' shape the rule names."}
{"row_id": "DI-007", "verdict": "FAIL", "severity": "blocker", "evidence": "Four verified product-versus-spec contradictions. (1) README.md:402-413 documents that 'A completed 25-minute unbroken focus block earns one star' and four `focus.*` knobs, but no product code can award one: `createMachine` (lib/focus/blocks.js:74), `createFrontmostBreaker` (frontmost.js:41), `createDisplayBreaker` (display.js:38), `createVideoBreaker` (video.js:33) and `createLatency` (lib/latency.js:17) are called only from test/, lib/daemon.js:22,34 opens the star store but never calls `award`, and `window.__latency` / `window.__stars.forfeit` in web/panel.html:1766,1795 have no caller outside the Playwright tests. (2) README.md:339 states the panel default is 440x620 while config/interstice.config.default.json:29-30 is 640x900. (3) .github/workflows/ci.yml:21-22 runs `test ! -f package-lock.json` as a required step while package-lock.json is tracked in git, so CI exits 1 on every push and never installs the @playwright/test devDependency the five .pw.mjs tests import. (4) docs/FOCUS_MODULE_CONTRACTS.md:14,46 specifies `Machine.onTick(nowISO)` and `createTicker({onTick,onClear})`, while the implementations export `createMachine().send(event)` (blocks.js:74-95) and `createLatency().onSubmit/onComplete` (latency.js:17-40), and the contract's `breakers/lock.js` ships as breakers/display.js.", "fix": "Wire the machine, the three breakers and the latency mapper into lib/daemon.js's tick so a star can actually be awarded and the panel is fed real events; correct README.md:339 to 640x900; replace the ci.yml zero-dependency step with `npm ci` (or drop the tracked lockfile); and re-sync docs/FOCUS_MODULE_CONTRACTS.md to the shipped signatures."}
{"row_id": "DI-008", "verdict": "PASS", "severity": "high", "evidence": "Repository state at audit time, surfaced rather than worked around: branch main, `git rev-list --left-right --count origin/main...HEAD` = 0 0 (neither ahead nor behind), `git stash list` empty, no .git lock files, two tracked files modified (docs/RECURRING_GOALS_SELECTION.md, docs/recurring_goals_selection.py) and three untracked paths (UX_FEEDBACK.md, docs/audit/, docs/__pycache__/). The one anomaly worth naming is docs/__pycache__/, a build artifact that is untracked and absent from .gitignore.", "fix": ""}
{"row_id": "DI-009", "verdict": "NA", "severity": "blocker", "evidence": "No stash exists or was created: `git stash list` in /Users/scottlydon/Developer/interstice returns empty, and no `git stash` invocation appears in any script in the repo (bin/, hooks/, docs/audit/*.py, .github/workflows/ci.yml). The rule has no subject in this target.", "fix": ""}
{"row_id": "DI-010", "verdict": "FAIL", "severity": "high", "evidence": "README.md, which is neither a dated review record nor a log, quotes shipped copy and shipped values with no generator: README.md:281 quotes the product string 'AnkiConnect unreachable: fetch failed' emitted at lib/state/anki.js:46, and no test in test/ pins that string; README.md:335-346 reproduces the config defaults, and that quote has already drifted (README.md:339 `\"width\": 440, \"height\": 620` against config/interstice.config.default.json:29-30 `640` / `900`). package.json has no sync, gen or build script, and no script in the repo rewrites README.md from source.", "fix": "Add a generator (for example `npm run docs:sync`) that rewrites the README configuration block from config/interstice.config.default.json and the quoted error strings from lib/, or add a test that asserts each quoted string still exists at its source."}
{"row_id": "PS-001", "verdict": "PASS", "severity": "high", "evidence": "The project root is /Users/scottlydon/Developer/interstice, under ~/Developer and outside any iCloud-managed folder. Nothing in the repo directs a build or scratch path into an iCloud folder: config/interstice.config.default.json and lib/paths.js resolve logs and config under the repo and ~/Library, and the only absolute paths in tooling (docs/recurring_goals_selection.py:33-34) also point under ~/Developer.", "fix": ""}
{"row_id": "PS-002", "verdict": "PASS", "severity": "high", "evidence": "Atlas knows the project: /Users/scottlydon/Developer/atlas/data/projects.json carries an entry with id 'interstice', path /Users/scottlydon/Developer/interstice, `\"summary\": \"Fills the dead moment after you dispatch an AI agent, before you can decide to waste it.\"` and `\"summarySource\": \"readme\"`, and data/groups.json assigns it to the 'agent-tooling' function family.", "fix": ""}
{"row_id": "PS-003", "verdict": "PASS", "severity": "medium", "evidence": "README.md:1-3 opens with what the project is ('Fills the dead moment after you dispatch an AI agent, before you can decide to waste it.'), README.md:48-64 gives the run instructions (requirements, clone, doctor, install, start), and README.md:350-381 is an explicit honest-gaps table of what will silently break and what handles it.", "fix": ""}
{"row_id": "PS-004", "verdict": "FAIL", "severity": "medium", "evidence": "No graphify knowledge graph or auto-update is installed for this project: `ls graphify-out` in the repo returns 'No such file or directory', .git/hooks/ contains no post-commit hook (only the shipped .sample files), and ~/Library/LaunchAgents holds com.graphify.watch.factory.plist and com.interstice.daemon.plist but no graphify watcher for interstice.", "fix": "Run `graphify-hybrid /Users/scottlydon/Developer/interstice --build` to build the graph and install the post-commit hook plus the per-project watcher LaunchAgent."}
{"row_id": "PS-005", "verdict": "PASS", "severity": "high", "evidence": "All four roles are filled and each document predates the code it governs. What is being built: README.md and docs/design-brief.html. How it is structured: docs/FOCUS_MODULE_CONTRACTS.md (committed db961a7, 08-19 06:37) and docs/design-immersive-reading.html. What correct behavior is: docs/GOAL_LOOP.md success conditions and per-item verifies. Slice list: the phase checklists in docs/GOAL_LOOP.md and docs/GOAL_LOOP_STARS_READER_2026-08-17.md. The scaffold commit db6b7f6 (07-30 13:21, 'MIT license, README, package manifest, design docs') precedes the first feature commit 443368c (13:25), and the module contracts precede the first lib/focus commit fcdb7d3 (06:51).", "fix": ""}
{"row_id": "PS-006", "verdict": "PASS", "severity": "high", "evidence": "The active loop file exists at /Users/scottlydon/Developer/interstice/docs/GOAL_LOOP_STARS_READER_2026-08-17.md and its path is cited as the binding authority in CLAUDE.md:5-7, which also names the RULE block governing edits to it. State changes are written back: docs/STARS_NOTES_2026-08-19.md is the linked parallel-notes file the loop's RULE permits, and it records per-item outcomes (0.3b, 1.2-1.6, 8.2, 9.3).", "fix": ""}
{"row_id": "PS-007", "verdict": "FAIL", "severity": "high", "evidence": "docs/audit/route.py, a pipeline that reads another project (/Users/scottlydon/Developer/Recurring_goals) and writes batches for this one, hardcodes this repo's file clusters: line 26 `\"web/panel.html, web/dashboard.html, web/learn.html\"`, line 30 `\"lib/focus/**, lib/video/**, lib/latency.js, lib/reader.js\"`, line 32 `\"lib/server.js, lib/daemon.js, lib/panel.js, lib/router.js, bin/, hooks/\"`. One of those hardcoded values is already wrong: web/ contains only dashboard.html, panel.html and debug.html, so the ux-b batch names a file that does not exist. docs/recurring_goals_selection.py:33-34 is only half agnostic: env overrides exist but default to the absolute paths /Users/scottlydon/Developer/Recurring_goals and /Users/scottlydon/Developer/interstice.", "fix": "Derive the batch localities at runtime (glob the target repo's source tree and cluster by directory) and take the target repo path as a required argument, so the router works on any repo; make RECURRING_GOALS_ROOT and TARGET_REPO required rather than defaulted to one machine's paths."}
